<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teste_tcc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ornatis">
<!--        <activity-->
<!--            android:name=".ConfiguracaoConta"-->
<!--            android:exported="false" />-->
        <!-- <activity -->
        <!-- android:name=".HomePrestador" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".CadastroFuncionamento" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".CadastroDadosLogin" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".CadastroRegrasDeNegocio" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".CadastroRecebimento" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".CadastroDadosDeLocalizacao" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".CadastroPerfilAdministrador" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".CadastroPerfilEstabelecimento" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".CadastroDadosLogin"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>